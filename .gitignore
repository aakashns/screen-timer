build/
.DS_Store

# Xcode. ScreenTimer.xcodeproj is generated from ios/project.yml but committed
# anyway, so the project can be opened without installing xcodegen.
xcuserdata/
DerivedData/
*.xcuserstate
